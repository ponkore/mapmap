(ns mapmap.views.geojson-util-test
  (:require [mapmap.views.geojson-util :refer :all]
            [midje.sweet :refer :all]))

(fact "to-station-feature-map"
  )

(fact "to-line-feature-map"
  )

(fact "make-feature-collection"
  )
